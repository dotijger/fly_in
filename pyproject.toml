[project]
name = "fly_in"
version = "0.1.0"
description = "Codam Core Curriculum: Fly-In"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["mypy>=2.3.0", "pydantic>=2.13.4"]

[tool.mypy]
explicit_package_bases = true
mypy_path = "."
